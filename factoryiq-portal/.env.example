# Minimal local (no Docker) configuration
DATABASE_URL=sqlite:///./factoryiq.db
SECRET_KEY=change-this-to-a-long-random-secret
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=480
SMTP_FROM=no-reply@factoryiq.local

# Optional overrides (uncomment only if you use these services locally)
# REDIS_URL=redis://localhost:6379/0
# MINIO_ENDPOINT=localhost:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET=factoryiq-docs
# Example PostgreSQL URL (local service):
# DATABASE_URL=postgresql+psycopg2://factoryiq:<password>@localhost:5432/factoryiq
